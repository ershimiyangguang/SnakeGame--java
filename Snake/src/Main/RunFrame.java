package Main;

import javax.swing.*;

public class RunFrame extends JFrame {

    JFrame runFrame=new JFrame();


    public void init(){

        runFrame.setSize(941, 664);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        runFrame.setVisible(true);
    }
}
